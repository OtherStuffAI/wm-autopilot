# Exact FIPS origins in custom signing policy

Task ce047a17-8f31-4c3f-98a7-4b1151a98ac9, workspace 2e5caefd-dd65-45d2-b747-ee874e8e5fc9. Origin message e6272678-db9d-4a0c-af99-deb95cd28585 in Wingman Suite channel855d79ef-94cd-4661-9f45-f9ce225f69c4 thread7f8c9eda-6e1a-49fe-b355-821632d340bd. Pete requested Autopilot access and FD/Tower gateway over FIPS now using supervised workers; HTTPS preserved, discovery deferred. Manager active, owns chat. Previous Autopilot commit2cc1ec7 source accepted with 160 tests + independent51; no runtime restart approved yet.

New concrete integration defect: live Tower FIPS gateway is up at http://npub109684nue495hq240u3dqzyf2kltk23u3mqkk9l44ga6szed4jcysramf74.fips:43100 . Manager current capability denies mesh origin. Preparing a narrow custom signing policy fails validateSigningPolicyDraft in src/signing/signing-policy-validation.ts:38: custom origins must be HTTPS, so the supported policy UI/API cannot grant the required encrypted HTTP FIPS endpoint at all. Reviewable draft /Users/mini/wingmen/wingman21/mynotes/fips-manager-signing-policy.json reproduces validation failure. Current manager capability also cannot access admin signing policy paths; do NOT try to self-grant or bypass this boundary.

Write scope ONLY Autopilot repo. Implement narrow supported custom NIP98 target validation for canonical checksummed lowercase http://<npub>.fips:<explicit-valid-port> origins, in addition to existing HTTPS origins. Reject arbitrary plaintext HTTP, localhost/IP, malformed/fake npub, wildcard, userinfo/path/query/hash and unsafe port forms. Reuse existing canonical npub logic if available. Preserve policy assignments/path/method/body-hash enforcement; do not auto-add default origins, self grant, change live policy or weaken admin management gates. Update relevant type/UI help/error docs if needed for coherent usable policy entry. Verify draft validates and negative cases remain rejected with meaningful tests. Add small operator note to docs/fips-autopilot-ingress.md about explicit policy assignment/reissue/adoption for agent mesh signing. No raw keys. No Autopilot restart/push; same pending approved restart will activate both commits. Commit main compatible tested state preserving concurrent edits. Read repo AGENTS and latest task/comments first and at handoff. Report tests/commit/exact remaining grant+reissue steps to task and final callback. Broker PG helper at /Users/mini/wingmen/wingman21/mycode/fips-delivery-pg.mjs. Do not post final chat. Set session goal/reflect; stop at real handoff. Small bounded pickup, not a signing-system redesign.
