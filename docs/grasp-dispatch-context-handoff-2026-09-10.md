# Restore supervised worker Flight Deck and broker context

Tracking task: `<task-id>` in the verified workspace/channel. The full operational brief is preserved on the Flight Deck task and in the manager’s local brief; this public copy uses placeholders. Explicitly manager-routed implementation; latest manager execution contract is your brief, not a reason to skip self-authored dispatch.

The operator authorised building the GRASP FIPS PoC in `<launch-instruction>`; this is the demonstrated Autopilot integration prerequisite for `<poc-task>`. Implement in `<autopilot-repo>`, main, supervised session; do not restart Autopilot. Read latest architecture v5 scene in `<architecture-scene-path>` and repo instructions.

Confirmed symptom: manager session `<manager-session-id>` dispatched worker `<worker-session-id>` via `<dispatch-id>`. Worker has role=dispatched-worker, AGENT=false, agentProfileId and agentChatBotNpub, but flightdeck_context returns empty backend/app/workspace/bot unavailable. Manager context works. Manager metadata writes against worker denied 403 admin-or-execution-delegation-required, owner-space denied active-owner-delegation-required; log/metadata reads work, worker self goal update works. Source src/session-dispatch/session-dispatch-service.ts create passes only role and callbackSessionId to manager.createSession, while reportingContext is saved separately. Investigate exact cause; do not assume AGENT is sole fix or blindly copy all parent metadata.

Authoritative manager context: backend `<tower-url>`; app `<app-npub>`; subscription `<subscription-id>`; backend connection `<backend-connection-id>`; Tower `<tower-service-npub>`; workspace `<workspace-id>`; scope `<scope-id>`; channel `<channel-id>`; thread `<thread-id>`. Stable bot identity `<bot-npub>`. Operator owner `<owner-npub>`.

Goal: narrow, tested context inheritance for supervised worker to use existing stable broker identity and explicit Flight Deck workspace/task context, preserving callback exact-session semantics and no mandatory Flight Deck dependency for ordinary dispatch. Prevent forged reportingContext from becoming authority; verify trusted parent metadata and owner. Preserve default no user-thread duplicate publishing. Never copy private keys, use raw-key auth, broaden broker grants or silently sign as the operator. Do not modify unrelated code. Preserve concurrent work, inspect full worktree, commit only scoped tested changes, no force/reset/rebase.

Acceptance: unit/regression tests for trusted context inheritance, ordinary dispatch without Flight Deck, cross-owner rejection, no forged context privilege escalation, correct worker bot context and task reads where live safe verification possible. Run affected session-dispatch tests plus native required checks. Distinguish source-tested from running process. If supported worker-self metadata repair can unblock current worker safely without Autopilot restart, provide exact command/schema and evidence; do not mutate the other worker directly. Return a concrete restart requirement only if necessary after tests/commit; manager will request operator approval then. No restart authorised.

Set own goal/reflect. Use this self-contained brief even if this worker suffers same missing context. Post task progress if available; otherwise durable local docs and supervised callback to manager are sufficient, manager updates task and originating thread. Return exact diagnosis, paths/diff/commits/tests and runtime activation steps. No pipelines. Independent review follows implementation.
