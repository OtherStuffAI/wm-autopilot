# Compatibility overlay retained for existing deployment commands. FIPS is now
# provisioned by docker-compose.yml itself, so this file intentionally adds no
# settings and can be removed from operator commands after an upgrade.
services:
  wingman: {}
